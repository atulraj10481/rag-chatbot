import { DocumentChunk } from '@/types';

export async function retrieveChunks(query: string): Promise<DocumentChunk[]> {
  console.log("Retrieve chunks placeholder for query:", query);
  return [];
}
