// OpenRouter native REST API client placeholder
export class OpenRouterClient {
  private apiKey: string;
  constructor(apiKey: string) {
    this.apiKey = apiKey;
  }
  async complete() {
    console.log("OpenRouter client placeholder");
  }
}
